openai>=1.55.3
python-dotenv>=0.19.0
requests>=2.25.0
tqdm>=4.65.0
tiktoken>=0.8

# dataset download / preprocessing (get_data/)
datasets>=2.14.0
beautifulsoup4>=4.12.0

# factscore (atomic fact decomposition + retrieval)
nltk>=3.8,<3.10  # 3.10+ blocks importing its dependencies (e.g. `regex`) when they resolve
                 # inside the current working directory (CWE-427 hardening) -- this trips up
                 # the common "python -m venv .venv" (venv nested inside the repo) workflow
rank-bm25>=0.2.2
numpy>=1.24.0

# analysis / scoring / revisions
pandas>=2.0.0
matplotlib>=3.7.0
scipy>=1.10.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
